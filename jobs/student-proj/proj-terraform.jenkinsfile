pipeline{
  agent any
  stages {

    stage('clone repo') {
      steps{
        git credentialsId: 'git', url: 'https://github.com/cheerlavamsi/terraform.git'
            
      }  
    }  

    stage('run terraform code') {
      steps{
        sh '''
            cd Project-01
            make dev-apply
        '''

      }  
    }


  }
}